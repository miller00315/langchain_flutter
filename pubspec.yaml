name: langchain_studies
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  
  langchain: ^0.7.4
  chromadb: ^0.2.0+1
  cupertino_icons: ^1.0.6
  langchain_openai: ^0.7.0
  langchain_chroma: ^0.2.1+1
  openai_dart: ^0.4.0
  googleai_dart: ^0.1.0+2
  ollama_dart: ^0.2.0
  langchain_community: ^0.3.0
  langchain_google: ^0.6.1
  objectbox: ^4.0.1
  langchain_core: ^0.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true